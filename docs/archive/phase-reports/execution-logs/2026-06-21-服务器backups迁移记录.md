# 服务器 backups 迁移记录

## 1. 基本信息

- 操作时间：2026-06-21
- 操作目标：处理生产服务器仓库内未跟踪 `backups/` 目录，让服务器 Git 工作区恢复干净
- 服务器项目目录：`/www/alibaba-ai-assistant`
- 操作前服务器分支：`main`
- 操作前服务器 HEAD：`8c66f46563f92e3044e2a6d5d034814033cfb3d4`
- 本地 main / origin/main：`203d1484c5c3c826f83ed28162133df9a363848a`
- Phase 1D 代码目标：`494bd37f3c155b7ecf64ac0fbced517118b1d66e`

本阶段未执行：

- 未部署
- 未执行服务器 `git pull`
- 未执行 `npm install` / `npm ci`
- 未执行 `npm run build`
- 未执行 PM2 restart / reload / stop / delete
- 未修改业务代码
- 未修改数据库
- 未删除 `backups/`
- 未使用 `git clean`
- 未读取、打印、复制 `.env.local`
- 未调用真实 AI

## 2. 操作前状态

只读检查命令：

- `pwd`
- `git status -sb`
- `git branch --show-current`
- `git rev-parse HEAD`

结果：

| 检查项 | 结果 |
| --- | --- |
| 当前目录 | `/www/alibaba-ai-assistant` |
| 当前分支 | `main` |
| 当前 HEAD | `8c66f46563f92e3044e2a6d5d034814033cfb3d4` |
| 操作前 git status | `## main...origin/main`，并存在 `?? backups/` |

判断：目录、分支、HEAD 均符合部署前预演记录；阻塞项确认为仓库内未跟踪 `backups/`。

## 3. backups 元信息

只查看文件名、层级和大小，未打开文件内容。

执行命令：

- `ls -ld backups`
- `find backups -maxdepth 2 -type f | sed 's#^#- #' | head -50`
- `du -sh backups`

结果：

| 项目 | 结果 |
| --- | --- |
| 原路径 | `/www/alibaba-ai-assistant/backups` |
| 目录权限与时间 | `drwxr-xr-x 2 root root 4096 Jun 20 15:43 backups` |
| 文件列表 | `backups/prod.db.20260620-154309.bak` |
| 大小 | `348K` |

判断：`backups/` 为历史数据库备份目录，未发现需要读取内容才能判断的异常文件。

## 4. 迁移操作

仓库外目标目录：

```text
/www/server-backups/alibaba-ai-assistant/2026-06-21-before-phase1d-deploy/backups
```

执行方式：

1. 创建仓库外目录：`/www/server-backups/alibaba-ai-assistant/2026-06-21-before-phase1d-deploy`
2. 使用 `mv` 将仓库内 `backups/` 整体迁移到仓库外目标路径。
3. 迁移前检查目标路径不存在，避免覆盖。

说明：

- 本次是迁移，不是删除。
- 未读取 `.bak` 文件内容。
- 未修改数据库。
- 未执行 `git clean`。

## 5. 操作后状态

迁移后检查：

| 检查项 | 结果 |
| --- | --- |
| 迁移目标路径 | `/www/server-backups/alibaba-ai-assistant/2026-06-21-before-phase1d-deploy/backups` |
| 迁移后目录存在 | 是 |
| 迁移后大小 | `348K` |
| 操作后 git status | `## main...origin/main` |
| 仓库根目录是否仍有 `backups/` | 否 |
| 服务器 Git 工作区是否干净 | 是 |

## 6. 生产状态复核

只读检查命令：

- `pm2 status`
- `curl -s http://127.0.0.1:3005/api/health`
- `curl -I http://127.0.0.1:3005/`
- `curl -I http://127.0.0.1/`

结果：

| 检查项 | 结果 |
| --- | --- |
| PM2 应用 | `alibaba-ai-assistant` |
| PM2 状态 | `online` |
| `/api/health` | `{"ok":true}` |
| `127.0.0.1:3005/` | 200 OK |
| `127.0.0.1/` | 200 OK |

判断：迁移 `backups/` 后，生产服务仍正常。

## 7. 风险与结论

### P0

- 无。生产访问检查正常。

### P1

- 无。服务器 Git 工作区已恢复干净。

### P2

- 无新增。

### P3

- 旧 mock 历史任务记录正文存在 `?????` 占位/乱码，仍建议 Alpha 演示前单独清理；不影响本次 `backups/` 迁移。

## 8. 限制确认

| 项目 | 结果 |
| --- | --- |
| 是否删除文件 | 否 |
| 是否部署 | 否 |
| 是否执行 `git pull` | 否 |
| 是否执行 npm install / build | 否 |
| 是否重启 PM2 | 否 |
| 是否修改业务代码 | 否 |
| 是否修改数据库 | 否 |
| 是否读取密钥 | 否 |
| 是否调用真实 AI | 否 |

## 9. 下一步建议

建议进入正式部署。

理由：

- 服务器当前仍在 `8c66f46563f92e3044e2a6d5d034814033cfb3d4`，需要后续部署到 Phase 1D 目标提交。
- 阻塞正式部署的 `backups/` 工作区污染已处理。
- 服务器 Git 工作区已干净。
- PM2 与生产访问复核正常。

正式部署前仍建议先备份 SQLite 数据库到仓库外路径，再执行部署。


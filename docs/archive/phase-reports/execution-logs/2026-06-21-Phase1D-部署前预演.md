# Phase 1D 部署前预演

## 1. 基本信息

- 预演时间：2026-06-21
- 本地工作目录：`C:\Users\a2578\Desktop\Workspace\projects\project-001-跨境电商AI工具\电商工具`
- 本地当前分支：`main`
- 本地 main HEAD：`494bd37f3c155b7ecf64ac0fbced517118b1d66e`
- origin/main HEAD：`494bd37f3c155b7ecf64ac0fbced517118b1d66e`
- 本地工作区：干净
- 本阶段目标：只做部署前预演，不真正部署

本阶段未执行：

- 未执行服务器 `git pull`
- 未执行 `npm install` / `npm ci`
- 未执行 `npm run build`
- 未执行 PM2 restart / reload / stop / delete
- 未修改服务器文件
- 未修改数据库
- 未读取、打印、复制 `.env.local`
- 未调用真实 AI

## 2. 本地最终状态确认

执行命令：

- `git status -sb`
- `git branch --show-current`
- `git rev-parse HEAD`
- `git ls-remote origin main`

结果：

| 检查项 | 结果 |
| --- | --- |
| 当前分支 | `main` |
| 本地 HEAD | `494bd37f3c155b7ecf64ac0fbced517118b1d66e` |
| origin/main | `494bd37f3c155b7ecf64ac0fbced517118b1d66e` |
| 工作区 | 干净 |

结论：本地 main 与远端 main 在预演开始时一致。

## 3. 生产服务器只读检查

服务器项目目录：`/www/alibaba-ai-assistant`

只读命令范围：

- `pwd`
- `git status -sb`
- `git branch --show-current`
- `git rev-parse HEAD`
- `git log --oneline -5`
- `pm2 status`
- `ss -lntp`
- `systemctl is-active nginx`
- `curl -I http://127.0.0.1:3005/`
- `curl -s http://127.0.0.1:3005/api/health`
- `curl -I http://127.0.0.1/`
- `curl -s http://127.0.0.1/api/health`

### 3.1 Git 状态

| 检查项 | 结果 |
| --- | --- |
| 服务器项目目录 | `/www/alibaba-ai-assistant` |
| 服务器当前分支 | `main` |
| 服务器当前 HEAD | `8c66f46563f92e3044e2a6d5d034814033cfb3d4` |
| 服务器工作区是否干净 | 否，存在未跟踪目录 `backups/` |
| 目标部署 HEAD | `494bd37f3c155b7ecf64ac0fbced517118b1d66e` |
| 当前生产是否已是目标 commit | 否 |

服务器最近提交：

```text
8c66f46 docs: update project readme for opportunities MVP
cb18f37 fix: align opportunities risk display
2665382 feat: add opportunities radar MVP
b1b8706 feat: improve onboarding demo confidence and task decisions
a10889b fix: unify access password and persist local drafts
```

判断：

- 生产服务器当前还不是 Phase 1D 目标提交。
- 服务器需要部署更新。
- 但服务器 Git 工作区存在未跟踪 `backups/`，按本阶段规则标记为 P1，正式部署前需要先确认或处理。

### 3.2 PM2、端口与 Nginx

| 检查项 | 结果 |
| --- | --- |
| PM2 应用名 | `alibaba-ai-assistant` |
| PM2 状态 | `online` |
| PM2 uptime | 约 `16h` |
| 应用监听 | `127.0.0.1:3005` |
| Nginx 监听 | `0.0.0.0:80` |
| Nginx 状态 | `active` |
| 3006 / 3011 | 未发现相关监听 |

### 3.3 生产访问状态

| 检查项 | 结果 |
| --- | --- |
| `127.0.0.1:3005/` | 200 OK |
| `127.0.0.1:3005/api/health` | `{"ok":true}` |
| `127.0.0.1/` | 200 OK |
| `127.0.0.1/api/health` | `{"ok":true}` |
| 公网 `http://112.124.54.81/` | 200 OK（使用 `curl.exe --noproxy "*"` 复核） |
| 公网 `http://112.124.54.81/api/health` | `{"ok":true}`（使用 `curl.exe --noproxy "*"` 复核） |

说明：

- PowerShell `Invoke-WebRequest` 首次访问公网 IP 返回过 502，但 `curl.exe --noproxy "*"` 与端口连通性复核均正常，判断更像本机代理/网络链路误判。
- 本次不标记生产访问 P0。

## 4. 部署差距判断

| 项目 | 当前值 |
| --- | --- |
| 服务器当前 HEAD | `8c66f46563f92e3044e2a6d5d034814033cfb3d4` |
| 目标 Phase 1D HEAD | `494bd37f3c155b7ecf64ac0fbced517118b1d66e` |
| 是否需要部署 | 是，需要从 `8c66f46` 更新到 Phase 1D |
| 是否已满足直接部署条件 | 否，服务器工作区存在未跟踪 `backups/` |

结论：**需要部署更新，但暂缓正式部署，需先处理问题。**

## 5. 数据库风险判断

检查范围：

- `prisma/schema.prisma`
- `prisma/migrations`
- Phase 1D 范围内的运行时写库逻辑

结果：

- Phase 1D 相对 `6640f7513f7f87d88dff641695e6bf71a0974e96` 未修改 `prisma/schema.prisma`。
- Phase 1D 未新增 migration 文件。
- Phase 1D 未引入数据库结构变化。
- Phase 1D 未新增数据清理脚本。
- Phase 1D 主要业务代码变化集中在：
  - `app/api/agents/risk/route.ts`
  - `app/api/agents/sourcing/route.ts`
  - `lib/server/alphaSafety.ts`

判断：

| 检查项 | 结论 |
| --- | --- |
| 是否需要 migrate | 否 |
| 是否建议备份数据库 | 是，部署前仍建议备份 SQLite 数据库 |
| 是否需要清理旧 mock 乱码数据 | 否，不在本次部署前执行；Alpha 演示前单独处理 |

备份建议：

- 优先备份到仓库外目录，避免再次污染 Git 工作区。
- 建议路径示例：`/www/backups/alibaba-ai-assistant/dev.db.phase1d-before-494bd37-YYYYMMDD-HHMMSS.bak`
- 如果继续使用仓库内 `backups/`，需要先确认该目录已被安全忽略或不会影响 `git status`。

## 6. 回滚方案预演

只写方案，不执行。

| 项目 | 值 |
| --- | --- |
| 当前生产 HEAD | `8c66f46563f92e3044e2a6d5d034814033cfb3d4` |
| 目标部署 HEAD | `494bd37f3c155b7ecf64ac0fbced517118b1d66e` |
| 是否涉及数据库结构迁移 | 否 |
| 回滚前提 | 已备份 SQLite 数据库；未执行结构迁移 |

回滚方式：

```bash
cd /www/alibaba-ai-assistant
git reset --hard 8c66f46563f92e3044e2a6d5d034814033cfb3d4
npm run build
pm2 restart alibaba-ai-assistant
```

回滚验证：

```bash
curl -s http://127.0.0.1:3005/api/health
curl -I http://127.0.0.1:3005/
curl -I http://127.0.0.1:3005/tasks
curl -I http://127.0.0.1:3005/sourcing
curl -I http://127.0.0.1:3005/risk
curl -I http://127.0.0.1/
curl -s http://127.0.0.1/api/health
```

注意：回滚命令本阶段未执行。

## 7. 正式部署步骤草案

只写步骤，不执行。

正式部署前先处理 P1：

1. 进入服务器项目目录：`cd /www/alibaba-ai-assistant`
2. 确认 `backups/` 是历史数据库备份目录，且不会覆盖目标 Git 文件。
3. 处理后确认 `git status -sb` 干净，或将备份目录迁移到仓库外路径。

正式部署建议步骤：

1. 进入服务器项目目录：`cd /www/alibaba-ai-assistant`
2. 确认 `git status -sb` 干净。
3. 记录部署前 HEAD：`git rev-parse HEAD`
4. 备份 SQLite 数据库文件到仓库外目录。
5. 执行：`git fetch origin`
6. 执行：`git pull --ff-only origin main`
7. 确认 HEAD 到目标提交。
8. 执行：`npm run build`
9. 执行：`pm2 restart alibaba-ai-assistant`
10. 验证本机应用：`http://127.0.0.1:3005`
11. 验证 Nginx 本机反代：`http://127.0.0.1`
12. 验证公网：`http://112.124.54.81`
13. 验证 `/api/health`
14. 做最小页面验证。
15. 记录部署结果。

## 8. 最小生产验证清单

正式部署后建议只做最小验证，不调用真实 AI。

页面：

- `/`
- `/tasks`
- `/sourcing`
- `/risk`
- `/summary`
- `/viral`
- `/products/new`
- `/materials`

接口：

- `/api/health` 应返回健康状态
- 无密码访问 `/api/tasks` 应返回 401 或受保护状态

验证边界：

- 不调用真实 AI
- 不提交表单生成 AI
- 不改数据库
- 不清理 mock 数据
- 不读取或打印 `.env.local`

## 9. 风险列表

### P0

- 无。公网访问经 `curl.exe --noproxy "*"` 复核为 200，`/api/health` 为 `{"ok":true}`。

### P1

- 服务器 Git 工作区存在未跟踪 `backups/` 目录。
  - 影响：按部署规则，工作区不干净时不建议直接执行正式部署。
  - 建议：正式部署前确认该目录用途，并迁移到仓库外备份目录或通过项目规则安全忽略。

### P2

- PowerShell `Invoke-WebRequest` 访问公网 IP 曾返回 502，但 `curl.exe --noproxy "*"` 复核正常。
  - 判断：更像本机代理/网络链路误判，不作为生产故障。
  - 建议：正式部署验证时优先使用 `curl --noproxy "*"` 或在服务器本机与外部各验证一次。

### P3

- 旧 mock 历史任务记录正文存在 `?????` 占位/乱码。
  - 是否阻塞部署：否。
  - 建议：Alpha 演示前单独做演示数据清理。

## 10. 结论

结论：**暂缓部署，需先处理问题。**

原因：

- 当前生产服务器不是 Phase 1D 目标提交，确实需要部署更新。
- PM2、Nginx、本机 3005、本机 80、公网访问和 `/api/health` 当前正常。
- Phase 1D 不需要数据库迁移。
- 但服务器 Git 工作区存在未跟踪 `backups/`，不满足“部署前工作区干净”的要求。

是否建议进入正式部署：暂不建议。先处理或确认 `backups/`，让服务器 `git status -sb` 干净后，再进入正式部署。

是否仍未真正部署：是，本阶段未真正部署。


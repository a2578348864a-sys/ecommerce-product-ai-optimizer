node_modules
.next
tsconfig.tsbuildinfo
out
.vercel
.env
.env.local
.env*.local
.env.*.local
.local/
.playwright-mcp/
# Local Playwright CLI session state and release staging
.playwright-cli/
.release-*/
release/
# Test temp artifacts
.tmp/
# Local smoke / audit / debug output
output/
# Browser profiles and private local states
.alpha-browser-profile/
**/.alpha-browser-profile/
.playwright-profile/
**/.playwright-profile/
radar-research/
radar-browser-profile/
*.har
*.log
cookies.json
storageState.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local Prisma SQLite database
prisma/dev.db
prisma/dev.db-journal
prisma/*.db
prisma/*.db-journal
# Local dated DB snapshots (e.g. dev.db.empty-tasks.20260806); pattern requires a
# dot suffix so the live prisma/dev.db is never masked.
prisma/dev.db.*

.vercel

# Local debug artifacts
.real_api_result.json
.api_result.json
.gen_result.txt
.mock_dev_*.txt
.real_dev_*.txt
.dev_*.txt
.start_check_*.txt
.build_*.txt
.page_test.html
.server_check.html
.test*.html
fix_*.py
test_*.py

# DB Protection — local backups and audit logs
.local-backups/
logs/

# Demo access credentials (contains password hashes — do not commit)
data/demo-access.json
# Demo sandbox data (contains demo user tasks/candidates — do not commit)
data/demo-sandbox.json
# Demo sandbox product batches (Visitor demo runtime data — do not commit)
data/demo-product-batches/
# Local env backups (may contain real secrets — never commit)
.env.local.bak-*
.env.local.bak-corrupt-*
# Env files by environment (currently absent — prevent accidental commits)
.env.production
.env.development
.env.test
# Local/demo security gate test artifacts
data/demo-sandbox.test-system-recovery3.json
# Private AI image drafts and request ledger
data/ai-image-drafts/
# Short-lived standalone Studio result manifests
data/studio-image-results/
data/studio-listing-results/

# Legacy purge DB backups (never committed)
prisma/backups/

# V4 Replay bundles (published demo artifacts, regenerable)
data/replay-bundles/

import { describe,it,expect } from "vitest";
import { normalizeSellerBlocks,mapSellerBlocksToCandidates } from "./mapping";
import { validateAiExtraction } from "./service";
const block={sourceBlockId:"bullet:0",section:"bullet" as const,label:"bullet",text:"Leak-proof stainless steel bottle, ideal for travel. Hand wash only.",sourceUrl:"https://www.amazon.com/dp/B000000000"};
describe("amazon fact enrichment",()=>{it("normalizes and maps seller content to canonical fields",()=>{const blocks=normalizeSellerBlocks([block,block]);const cs=mapSellerBlocksToCandidates({taskId:"t",asin:"B000000000",blocks});expect(blocks).toHaveLength(1);expect(cs.some(c=>c.field==="functional_feature")).toBe(true);expect(cs.some(c=>c.field==="use_scenario")).toBe(true);});it("rejects AI evidence absent from source",()=>{expect(()=>validateAiExtraction({candidates:[{field:"other",value:"strong claim",sourceBlockId:"bullet:0",evidenceText:"not present",qualifier:"direct",confidence:"high"}]},[block])).toThrow("evidence_not_found_in_source");});it("accepts exact evidence and marks compatibility review",()=>{const b={...block,text:"Compatible with standard cup holders."};const out=validateAiExtraction({candidates:[{field:"compatibility",value:"Compatible with standard cup holders",sourceBlockId:"bullet:0",evidenceText:"Compatible with standard cup holders.",qualifier:"direct",confidence:"high"}]},[b]);expect(out.candidates[0].field).toBe("compatibility");});});

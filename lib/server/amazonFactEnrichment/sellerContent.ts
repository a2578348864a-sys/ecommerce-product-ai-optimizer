import type { AmazonSellerContentBlockV1 } from "./contract";
import { normalizeSellerBlocks, normalizeSellerText } from "./mapping";
export type SellerContentDom = { querySelectorAll:(selector:string)=>ReadonlyArray<any>; querySelector:(selector:string)=>any|null };
function readNodes(root:SellerContentDom, selectors:string[], section:AmazonSellerContentBlockV1["section"], url:string):Partial<AmazonSellerContentBlockV1>[] { const out:Partial<AmazonSellerContentBlockV1>[]=[]; for(const selector of selectors){ for(const node of root.querySelectorAll(selector)){ const text=normalizeSellerText(node?.innerText ?? node?.textContent); if(!text) continue; const imageAlt=normalizeSellerText(node?.querySelector?.("img")?.getAttribute?.("alt"),300); out.push({sourceBlockId:`${section}:${out.length}`,section,label:section,text,sourceUrl:url,...(imageAlt?{imageAlt}:{})}); } } return out; }
export function extractAmazonSellerContent(root:SellerContentDom,pageUrl:string,entityBound:boolean):AmazonSellerContentBlockV1[]{ if(!entityBound) return []; const blocks=[...readNodes(root,["#feature-bullets li","#feature-bullets .a-list-item","#bullet_feature_div li"],"bullet",pageUrl),...readNodes(root,["#productDescription","#productDescription_feature_div","#bookDescription_feature_div"],"description",pageUrl),...readNodes(root,["#aplus","#aplus_feature_div","#aplus3p_feature_div","[data-a-name='aplus']"],"aplus",pageUrl)]; return normalizeSellerBlocks(blocks); }
export const SELLER_CONTENT_SELECTORS = ["#feature-bullets li","#feature-bullets .a-list-item","#bullet_feature_div li","#productDescription","#productDescription_feature_div","#bookDescription_feature_div","#aplus","#aplus_feature_div","#aplus3p_feature_div","[data-a-name='aplus']"] as const;


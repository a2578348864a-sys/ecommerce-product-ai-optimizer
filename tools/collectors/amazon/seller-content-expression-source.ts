export const SELLER_CONTENT_EXTRACTOR_SOURCE = `(() => { const clean=(v,m=1200)=>typeof v==='string'?v.replace(/[\\u0000-\\u001f\\u007f]/g,' ').replace(/\\s+/g,' ').trim().slice(0,m):''; const sels=[['bullet',['#feature-bullets li','#feature-bullets .a-list-item','#bullet_feature_div li']],['description',['#productDescription','#productDescription_feature_div','#bookDescription_feature_div']],['aplus',['#aplus','#aplus_feature_div','#aplus3p_feature_div',\"[data-a-name='aplus']\"]]]; const out=[]; let i=0; for(const [section,arr] of sels){ for(const sel of arr){ for(const n of document.querySelectorAll(sel)){ const text=clean(n.innerText||n.textContent); if(!text) continue; const alt=clean(n.querySelector&&n.querySelector('img')&&n.querySelector('img').getAttribute('alt'),300); out.push({sourceBlockId:section+':'+(i++),section,label:section,text,sourceUrl:location.href,...(alt?{imageAlt:alt}:{})}); } } } return out; })()`;
export function buildAmazonSellerContentExtractionExpression(){ return SELLER_CONTENT_EXTRACTOR_SOURCE; }

